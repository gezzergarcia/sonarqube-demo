pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh '/var/jenkins_home/tools/hudson.tasks.Maven_MavenInstallation/MAVEN_HOME/bin/mvn -B -DskipTests clean package'
            }
        }
    }
}
